/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router включён по умолчанию в Next.js 14
  // Опции для будущего: output: 'standalone' для Docker-деплоя
};

module.exports = nextConfig;

# Скопируйте в .env.local для локальной разработки.
# NEXT_PUBLIC_* переменные доступны и на сервере, и в браузере.

# URL бэкенда. По умолчанию — localhost:8000 (uvicorn / docker-compose)
NEXT_PUBLIC_API_URL=http://localhost:8000
